class : Base is Reader {
  Derived(int32 value): Base(value) {}

  override func Read(int32 left, int32 right): int32? {
    return Value + left + right;
  }

  override func Flush() { println("derived-flush"); }
}
