struct {
  byte tag;
  string Text;
  uint32 Code;

  Label(string text) {
    tag = 1;
    Text = text;
    Code = 7;
  }

  Label(string text, byte marker) {
    tag = marker;
    Text = text;
    Code = 7;
  }
}
