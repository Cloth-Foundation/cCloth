import Point;
import Transform;
import Packet;

class is Transform {
  Packet Cached = Packet("base");
  Projector() {}

  override func Apply(Point value): Point {
    value.X += 5;
    return value;
  }
}
