import greetings::Greeter;
import numbers::Value as Number;
import exports.*;

static func Main() {
    print(Greeter.Text());
    print(Number.Get());
    print(Banner.Text());
    print(Helper.Punctuation());
}
