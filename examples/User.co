string Name;
int32 id;

User(string name, int32 id) {
    self.Name = name;
    self.id = id;
}

func Find(int32 id): User? {
    return Repository.Find(id);
}

func validate(): bool {
    return id > 0;
}
